"use client";

import type { DiagnosisPillarResult } from "@/lib/diagnosis/types";

// Phase 1: Single pillar score card
// Shows: pillar name, score bar (0-4), narrative, gap indicators

// TODO: implement pillar visualization

export function PillarCard({ result }: { result: DiagnosisPillarResult }) {
  void result;
  return <div />;
}
